# Frappe Bench .gitignore
# Minimal ignores - track everything so you can see all changes

# Virtual environment
env/

# Logs
logs/
*.log

# Redis dumps
dump.rdb
*.rdb

# Site data (contains passwords and sensitive info)
sites/*/site_config.json
sites/*/private/
sites/*/public/files/
sites/*/locks/
sites/*/indexes/
sites/*/touched_tables.json

# Assets (can be rebuilt - don't track)
sites/assets/

# Config runtime files
config/pids/
config/bench_build.lock
config/site_config.lock
config/scheduler_process

# Python cache (keep __pycache__ ignored but track .py files)
__pycache__/
*.pyc
*.py[cod]

# Node modules (keep ignored, but track package.json changes)
node_modules/

# Temporary files
*.tmp
*.bak
*.backup

# IDE (optional - you may want to track these)
# .vscode/
# .idea/
*.swp
.DS_Store

# Build artifacts (source maps - you may want to track these)
# *.map

# Credentials and secrets
.git-credentials
.git-credentials-helper.txt
*.token
*.secret
.env
.env.local

# App git repositories (keep ignored - apps have their own git repos)
apps/*/.git/
apps/*/.git

# But track all app files so you can see changes to core files
# apps/frappe/** - TRACKED
# apps/erpnext/** - TRACKED  
# apps/hrms/** - TRACKED
# apps/irt_hrms/** - TRACKED
